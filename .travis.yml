language: python
python:
        - 3.8
before-install:
        - pip install poetry
install:
        - poetry install
script:
        - poetry run pytest
        - poetry run flake8 pyfunctory/
        - poetry run black --check pyfunctory/
